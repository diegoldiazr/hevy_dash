version: '3'

services:
  app:
    build: .
    image: hevy-dash:latest
    container_name: hevy-dash
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/app/data
    restart: always
